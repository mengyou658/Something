package com.moons.xst.track.common;

import android.app.Application;

public class AppGlobal extends Application {

	/*
	 * BY LKZ 2015.3.15
	 * android系统会为每个程序运行时创建一个Application类的对象且仅创建一个，所以Application可以说是单例
	 * (singleton)模式的一个类.且application对象的生命周期是整个程序中最长的，它的生命周期就等于这个程序的生命周期。
	 * 原则上一个运行时只创建一个，因此，将这里的代码合并到了AppContext.java中了。
	 */
}
